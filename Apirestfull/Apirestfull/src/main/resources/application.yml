spring:
  application:
    name: web-app-service
  datasource:
    url: jdbc:mysql://localhost:3306/demo_db
    username: root
    password: Jeison2270854
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
